[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "balance"
version = "0.1.0"
description = "Personal balance sheet tracker"
requires-python = ">=3.10"
dependencies = ["click>=8.0"]

[project.scripts]
balance = "balance.cli:balance"
